#include <iostream>

// Permutation (all possible strings using characters of that string)

/*
    string = "abc"

    "abc", "acb", "bac", "bca", "cab", "cba"
    possible permutations = 3! = 6
*/

void permutation(char str[])
{
    
}

int main()
{
    char str[] = "abc";

    std::cout << "all permutations of " << str << " is: ";
    permutation(str);

    return 0;
}